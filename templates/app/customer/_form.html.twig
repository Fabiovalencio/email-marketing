<div class="box-body">
    <div class="form-group">
        {{ formRow(form.get('name')) }}
    </div>
    <div class="form-group">
        {{ formRow(form.get('email') ) }}
    </div>
</div>
<div class="box-footer">
    {{ formButton(submit) }}
</div>