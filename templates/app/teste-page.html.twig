{% extends '@layout/default.html.twig' %}

{% block title %}Teste{% endblock %}

{% block content %}

    <div class="jumbotron">
        <h1>Welcome to <span class="zf-green"> {{  block('title') }} </span></h1>
        <p>
            {{ data.message }}
        </p>
        {% for cliente in clientes %}
        <p>
            ------------------------------------------------------------------<br/>
            ID: {{ cliente.id }}<br/>
            Nome: {{ cliente.nome }}<br/>
            E-mail: {{ cliente.email }}<br/>
            CPF: {{ cliente.cpf }}<br/>

            ==================================================================<br/>

            {% for endereco in cliente.endereco %}
                Endereço {{ loop.index }}:<br/>
                CEP: {{ endereco.cep }}<br/>
                Logradouro: {{ endereco.logradouro }}<br/>
                Cidade: {{ endereco.cidade }} - UF: {{ endereco.estado }}<br/>
            {% endfor %}
        </p>
        {% endfor %}
    </div>

{% endblock %}
